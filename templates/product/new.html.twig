{% extends 'base.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
    <div class="container">
        <div class="mt-5">

            <div class="">
                <a class="btn btn-secondary" href="{{ path('app_product_index') }}">back to list</a>
            </div>
            <hr>
            <h1 class="text-light">Create new Product</h1>

            {{ include('product/_form.html.twig') }}
        </div>

    </div>

{% endblock %}
